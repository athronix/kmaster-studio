93:        <n-input v-model:value="command" placeholder="command（如 npx / python）" size="small" />
103:.mcp-list { display: flex; flex-direction: column; gap: 10px; margin: var(--km-space-md) 0; }
104:.mcp-item { border: 1px solid var(--km-border); border-radius: var(--km-radius-lg); padding: 10px 12px; background: var(--km-panel); }
107:.mcp-meta { font-size: var(--km-font-sm); opacity: 0.7; margin: 6px 0; word-break: break-all; }
110:.mcp-form { display: flex; flex-direction: column; gap: var(--km-space-sm); border-top: 1px solid var(--km-border); padding-top: var(--km-space-md); }
